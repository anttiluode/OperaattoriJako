[project]
name = "operaattori-jako"
version = "0.1.0"
description = "Split compiled operator tangents into transport and nonlinear-feedback contributions."
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]

[tool.setuptools]
py-modules = ["jako", "fixed_point"]
